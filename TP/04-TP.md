### **TP 1 : Création de l'Entité, Contrôleur, Formulaire, Index et New 📋**

#### 1. **Création de l'Entité Article 🛠️**

Commande :
```bash
php bin/console make:entity
```

Donne à l'entité le nom `Article`, puis ajoute les champs suivants :
- `title` (type `string`)
- `content` (type `text`)
- `createdAt` (type `datetime`)

Exemple du code de l'entité :

```php
// src/Entity/Article.php
namespace App\Entity;

use Doctrine\ORM\Mapping as ORM;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * @ORM\Entity(repositoryClass=ArticleRepository::class)
 */
class Article
{
    /**
     * @ORM\Id()
     * @ORM\GeneratedValue()
     * @ORM\Column(type="integer")
     */
    private $id;

    /**
     * @ORM\Column(type="string", length=255)
     * @Assert\NotBlank(message="Le titre est obligatoire.")
     */
    private $title;

    /**
     * @ORM\Column(type="text")
     */
    private $content;

    /**
     * @ORM\Column(type="datetime")
     */
    private $createdAt;

    // Getters et setters ici...
}
```

---

#### 2. **Création du Contrôleur Article 📝**

Commande pour créer un contrôleur avec les actions `index` et `new` :
```bash
php bin/console make:controller ArticleController
```

Dans ce contrôleur, ajoute les actions pour lister (`index`) et créer (`new`) un article.

**Code du contrôleur** :

```php
// src/Controller/ArticleController.php
namespace App\Controller;

use App\Entity\Article;
use App\Form\ArticleType;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Annotation\Route;

class ArticleController extends AbstractController
{
    /**
     * @Route("/article", name="article_index")
     */
    public function index(): Response
    {
        $articles = $this->getDoctrine()->getRepository(Article::class)->findAll();

        return $this->render('article/index.html.twig', [
            'articles' => $articles,
        ]);
    }

    /**
     * @Route("/article/new", name="article_new")
     */
    public function new(Request $request, EntityManagerInterface $entityManager): Response
    {
        $article = new Article();
        $form = $this->createForm(ArticleType::class, $article);

        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            $article->setCreatedAt(new \DateTime());
            $entityManager->persist($article);
            $entityManager->flush();

            $this->addFlash('success', 'L\'article a été créé avec succès !');
            return $this->redirectToRoute('article_index');
        }

        return $this->render('article/new.html.twig', [
            'form' => $form->createView(),
        ]);
    }
}
```

---

#### 3. **Création du Formulaire Article 📄**

Commande pour créer un formulaire pour l'entité `Article` :
```bash
php bin/console make:form ArticleType
```

Code du formulaire :

```php
// src/Form/ArticleType.php
namespace App\Form;

use App\Entity\Article;
use Symfony\Component\Form\AbstractType;
use Symfony\Component\Form\Extension\Core\Type\TextType;
use Symfony\Component\Form\Extension\Core\Type\TextareaType;
use Symfony\Component\Form\Extension\Core\Type\SubmitType;
use Symfony\Component\Form\FormBuilderInterface;
use Symfony\Component\OptionsResolver\OptionsResolver;

class ArticleType extends AbstractType
{
    public function buildForm(FormBuilderInterface $builder, array $options)
    {
        $builder
            ->add('title', TextType::class, ['label' => 'Titre'])
            ->add('content', TextareaType::class, ['label' => 'Contenu'])
            ->add('save', SubmitType::class, ['label' => 'Enregistrer']);
    }

    public function configureOptions(OptionsResolver $resolver)
    {
        $resolver->setDefaults(['data_class' => Article::class]);
    }
}
```

---

#### 4. **Template Twig pour `Index` et `New` 💻**

**index.html.twig :**

```twig
{# templates/article/index.html.twig #}
<h1>Liste des Articles</h1>
<a href="{{ path('article_new') }}">Créer un nouvel article</a>

<ul>
    {% for article in articles %}
        <li>{{ article.title }}</li>
    {% endfor %}
</ul>
```

**new.html.twig :**

```twig
{# templates/article/new.html.twig #}
<h1>Créer un article</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Publier</button>
{{ form_end(form) }}
```

---

### **TP 2 : Création des Actions Edit et Delete ✏️❌**

#### 1. **Création de l'Action `edit` 🖊️**

Commande pour créer l'édition :
```bash
php bin/console make:controller ArticleController
```

**Code de l'action `edit` :**

```php
// src/Controller/ArticleController.php
    /**
     * @Route("/article/{id}/edit", name="article_edit")
     */
    public function edit(Request $request, Article $article, EntityManagerInterface $entityManager): Response
    {
        $form = $this->createForm(ArticleType::class, $article);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            $entityManager->flush();
            $this->addFlash('success', 'L\'article a été modifié avec succès !');

            return $this->redirectToRoute('article_index');
        }

        return $this->render('article/edit.html.twig', [
            'form' => $form->createView(),
        ]);
    }
```

---

#### 2. **Création de l'Action `delete` 🗑️**

Commande pour créer l'action de suppression :
```bash
php bin/console make:controller ArticleController
```

**Code de l'action `delete` :**

```php
// src/Controller/ArticleController.php
    /**
     * @Route("/article/{id}/delete", name="article_delete")
     */
    public function delete(Request $request, Article $article, EntityManagerInterface $entityManager): Response
    {
        if ($this->isCsrfTokenValid('delete'.$article->getId(), $request->get('_token'))) {
            $entityManager->remove($article);
            $entityManager->flush();
            $this->addFlash('success', 'L\'article a été supprimé avec succès !');
        }

        return $this->redirectToRoute('article_index');
    }
```

---

#### 3. **SoftDelete ⚠️**

Le concept de **SoftDelete** consiste à "marquer" un élément comme supprimé sans le supprimer réellement de la base de données (en utilisant un champ `deletedAt` ou `isDeleted`).

Voici un exemple de code pour activer un SoftDelete :

```php
// src/Entity/Article.php
/**
 * @ORM\Column(type="datetime", nullable=true)
 */
private $deletedAt;

// Fonction pour supprimer en SoftDelete
public function delete(): void
{
    $this->deletedAt = new \DateTime();
}
```

---

#### 4. **Twig pour `Edit` et `Delete` 🖼️**

**edit.html.twig :**

```twig
{# templates/article/edit.html.twig #}
<h1>Modifier l'article</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Mettre à jour</button>
{{ form_end(form) }}
```

**delete.html.twig :**

```twig
{# templates/article/delete.html.twig #}
<h1>Supprimer l'article</h1>
<form action="{{ path('article_delete', {'id': article.id}) }}" method="post">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button type="submit">Supprimer</button>
</form>
```

---

### **TP 3 : Commande `make:crud` 🔄**

Finalement, pour automatiser toutes ces étapes, vous pouvez utiliser la commande suivante :

```bash
php bin/console make:crud Article
```

Cela générera automatiquement :
- L'entité `Article`
- Le contrôleur `ArticleController`
- Le formulaire `ArticleType`
- Les templates Twig pour `index`, `new`, `edit`, `delete`

Cela va accélérer la création de ton CRUD en générant tout le nécessaire d'un coup ! 🎉
